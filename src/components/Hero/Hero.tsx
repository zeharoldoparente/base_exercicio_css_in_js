import { Form, Overlay, Container, HeroTitle } from './Hero.styles';

const Hero = () => (
  <Form>
    <Overlay />
    <Container>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </Form>
);

export default Hero;
